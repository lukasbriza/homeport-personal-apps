// Public API of the `home` feature — what other modules may import.
// Route entries (page / error / not-found) are imported directly by the matching
// route files in `app/`, not through this barrel.
export { Greeting } from './components/greeting'
