{{- if .Values.infisical.enabled }}
apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: {{ .Release.Name }}-secrets-sync
  namespace: {{ .Release.Namespace }}
spec:
  infisicalAuthRef:
    name: {{ .Values.infisical.authRef.name }}
    namespace: {{ .Values.infisical.authRef.namespace }}
  syncOptions:
    refreshInterval: 60s
  sources:
    - projectId: {{ .Values.infisical.projectId | quote }}
      environmentSlug: {{ .Values.infisical.environmentSlug }}
      secretPath: {{ .Values.infisical.secretPath }}
  targets:
    - name: {{ .Release.Name }}-secrets
      namespace: {{ .Release.Namespace }}
      kind: Secret
      creationPolicy: Owner
      # Infisical keys are KEYCLOAK_ADMIN_PASSWORD and KEYCLOAK_DB_PASSWORD
      # (matches the old .env.local naming), renamed here to the plain
      # ADMIN_PASSWORD / DB_PASSWORD keys the container env vars reference
      # (see deployment.yaml and postgres.yaml) — same rename pattern as
      # vaultwarden's chart.
      #
      # Note: the double-brace expression inside the backticks below is NOT
      # evaluated by Helm — Helm just prints it as literal text. It's
      # evaluated later, by the Infisical operator itself, against whatever
      # it fetched from your Infisical project.
      template:
        engineVersion: v1
        data:
          ADMIN_PASSWORD: "{{`{{ .KEYCLOAK_ADMIN_PASSWORD.Value }}`}}"
          DB_PASSWORD: "{{`{{ .KEYCLOAK_DB_PASSWORD.Value }}`}}"
{{- end }}
