{{- if .Values.infisical.enabled }}
apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: {{ .Release.Name }}-secrets-sync
  namespace: {{ .Release.Namespace }}
spec:
  infisicalAuthRef:
    name: {{ .Values.infisical.authRef.name }}
    namespace: {{ .Values.infisical.authRef.namespace }}
  syncOptions:
    refreshInterval: 60s
  sources:
    - projectId: {{ .Values.infisical.projectId | quote }}
      environmentSlug: {{ .Values.infisical.environmentSlug }}
      secretPath: {{ .Values.infisical.secretPath }}
  targets:
    - name: {{ .Release.Name }}-secrets
      namespace: {{ .Release.Namespace }}
      kind: Secret
      creationPolicy: Owner
      # Infisical's key is VAULTWARDEN_ADMIN_TOKEN (matches your .env naming), but the
      # container expects a plain ADMIN_TOKEN env var (see deployment.yaml). This picks
      # out just that one key and renames it — the other 3 keys in the /vaultwarden
      # folder (HOST_VAULTWARDEN_DATA_PATH, HOST_VAULTWARDEN_PORT) are compose-only and
      # never make it into the cluster.
      #
      # Note: the double-brace expression inside the backticks below is NOT evaluated
      # by Helm — Helm just prints it as literal text. It's evaluated later, by the
      # Infisical operator itself, against whatever it fetched from your Infisical project.
      template:
        engineVersion: v1
        data:
          ADMIN_TOKEN: "{{`{{ .VAULTWARDEN_ADMIN_TOKEN.Value }}`}}"
{{- end }}
